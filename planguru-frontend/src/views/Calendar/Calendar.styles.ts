import styled from 'styled-components';

export const Wrapper = styled.div`
  position: relative;
  height: 100%;
  display: flex;
`;

export const AddMeetingButtonWrapper = styled.div`
  position: absolute;
  bottom: 40px;
  right: 540px;
`;
