import styled from 'styled-components';

export const Wrapper = styled.div`
  display: flex;
  align-items: center;
  position: absolute;
  left: 108px;
  top: 0;
  right: 0;
  height: 108px;
  box-shadow: 0 4px 20px rgba(37, 38, 94, 0.1);
  background-color: rgba(255, 255, 255, 0.5);
  z-index: 3;
`;
